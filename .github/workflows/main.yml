name: Build with Gradle

on:
  push:
    branches: [ n-rootless ]
  pull_request:
    branches: [ n-rootless ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew build

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-artifacts
        path: app/build/

    - name: Upload debug apk
      uses: actions/upload-artifact@v4
      with:
        name: apk-debug
        path: |
          app/build/outputs/apk/debug/themeinterfacer.apk

    - name: Upload release apk
      uses: actions/upload-artifact@v4
      with:
        name: apk-rel
        path: |
          app/build/outputs/apk/release/themeinterfacer.apk
